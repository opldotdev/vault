{
  "name": "@opl.dev/vault",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "browser": "./dist/browser.js",
      "default": "./dist/index.js"
    }
  },
  "bin": {
    "vault": "./dist/cli/vault.js"
  },
  "files": [
    "dist",
    "swift/Sources",
    "swift/build.sh"
  ],
  "license": "MIT",
  "repository": "git+https://github.com/opldotdev/vault.git",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --format esm --target bun --packages external && bun build ./src/browser.ts --outfile ./dist/browser.js --format esm --target browser --packages external && bun build ./cli/vault.ts --outdir ./dist/cli --format esm --target bun --packages external && tsc -p tsconfig.build.json",
    "test": "bun test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "check": "bun run lint && bun test && bun run build"
  },
  "dependencies": {
    "@bsv/sdk": "^2.4.2",
    "bitcoin-backup": "^0.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
